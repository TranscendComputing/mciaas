{
	"required": {
		"source": {
			"type": "string",
			"description": "The path to a local file to upload to the machine. The path can be absolute or relative. If it is relative, it is relative to the working directory when Packer is executed."
		},
		"destination": {
			"type": "string",
			"description": "The path where the file will be uploaded to in the machine. This value must be a writable location and any parent directories must already exist."
		}
	}
}
